{
  "name": "opening-trainer-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "postinstall": "wrangler types"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "wrangler": "^4.140.0"
  }
}
